# Claims Desk — agent/tool-access conventions

Subagents spawned inside a dynamic workflow automatically inherit the
parent session's MCP tool permissions, including all connected MCP
servers — no special configuration required. Confirmed empirically in
Week 17 Day 1.
